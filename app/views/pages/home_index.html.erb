
<div class="jumbotron">
  <% if user_signed_in? %>
    <h1>Welcome</h1>
    <p class="lead">
      You have logged successfully!  If this were a real app some sweet content
      would go here.  But it's just a test.  You might want to sign out now.
    </p>
    <%= link_to "Log Out", destroy_user_session_path, :method => :delete, :class => 'btn btn-large btn-danger' %>
  <% else %>
    <h2>Welcome, new user!</h2>
    <p class="lead">
      You must sign in or sign up before you can access our super sweet,
      top secret content.
    </p>
    <div class="row-fluid">
      <div class="span6">
        <%= link_to "Sign In", new_user_session_path, :class => "btn btn-success" %>
      </div>
      <div class="span6">
        <%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-success" %>
      </div>
    </div>
  <% end %>
</div>
